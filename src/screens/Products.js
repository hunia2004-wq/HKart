import React from 'react';
import {Text, View} from 'react-native';

const Products = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>Products in Hkart</Text>
    </View>
  );
};

export default Products;